==============================================================================
		    Servidor Web

HOSTNAME.: "{{ ansible_fqdn }}"
ENDERECOIP.: "{{ ansible_all_ipv4_addresses }}"
DIST.: "{{ ansible_distribution }}" - "{{ ansible_distribution_version }}"
==============================================================================
